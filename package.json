{
  "name": "bopl-drafter",
  "version": "1.0.0",
  "scripts": {
    "start": "node server/dist/server.js",
    "postinstall": "npm install --prefix server && npm install --prefix client && npm run build --prefix client && npm run build --prefix server"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
